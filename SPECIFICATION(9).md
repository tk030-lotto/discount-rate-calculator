# 割引率らくらく計算 仕様書

## 1. 目的

本ツールは、通常価格と1つ以上の割引率から、各段階の割引後価格、最終支払額、および実質的な最終割引率を計算するためのWebツールとする。

特に、複数の割引を足し算してしまう誤解を避け、**割引を順番に適用する計算過程を分かりやすく表示すること**を目的とする。

## 2. スコープ

### 2.1 対象機能

- 通常価格の入力
- 1つ以上の割引率の入力
- 複数割引の順番指定
- 各割引適用後の価格計算
- 各段階の掛け率表示
- 最終支払額の表示
- 実質的な最終割引率の表示
- 計算過程の可視化
- スマートフォンを想定したUI

### 2.2 対象外

- ユーザーアカウント
- ログイン
- クラウド保存
- 商品データベース
- 店舗別割引ルール管理
- クーポン発行
- POS連携
- 外部サービス連携
- AI APIへの価格情報送信
- 税計算
- ポイント・還元率の計算
- 消費者向け価格調査

## 3. 入力仕様

### 3.1 通常価格

正の数値を入力する。

例：

```text
10000
```

### 3.2 割引率

1つ以上の割引率を入力できる。

例：

```text
50%
30%
```

複数の割引を入力した場合、入力された順番に適用する。

## 4. 割引計算仕様

### 4.1 単一割引

割引率を `r` とした場合、割引後価格は以下とする。

```text
割引後価格 = 現在価格 × (1 - r)
```

例：

```text
10,000円
50% OFF

10,000 × 0.50
= 5,000円
```

### 4.2 複数割引

複数の割引は加算せず、順番に適用する。

例：

```text
10,000円
↓ 50% OFF
5,000円
↓ 30% OFF
3,500円
```

内部計算としては、

```text
10,000 × 0.50 × 0.70
= 3,500
```

とする。

### 4.3 実質最終割引率

元価格を `P`、最終価格を `F` とした場合、

```text
最終割引率 = (1 - F ÷ P) × 100
```

で算出する。

例：

```text
元価格     10,000円
最終価格    3,500円

(1 - 3500 ÷ 10000) × 100
= 65%
```

## 5. 計算過程表示

複数割引を適用した場合、各段階の計算過程を表示する。

例：

```text
通常価格
10,000円

50% OFF
× 0.50
↓
5,000円

さらに30% OFF
× 0.70
↓
3,500円
```

各割引率だけでなく、**割引後の価格が次の計算の基準になっていること**が分かる表示を必須とする。

## 6. 勘違い防止表示

複数割引が入力された場合、合計割引率を単純加算した値を最終割引率として扱ってはならない。

例えば、

```text
50% OFF
+ 30% OFF
```

の場合、

```text
80% OFF
```

とは表示しない。

実際の最終割引率は、各割引を順番に適用した結果から算出する。

必要に応じて、

```text
50% + 30% = 80% ではありません
実際の最終割引率は 65% です
```

のような説明を表示できる構成とする。

## 7. 金額の丸め

金額表示は、日本円を基本とする。

計算途中で小数が発生した場合は、表示時に適切な金額単位へ丸める。

内部計算と表示上の丸めを分離し、可能な限り計算誤差を抑える。

## 8. 入力値検証

- 通常価格は0より大きい値のみ有効とする。
- 割引率は0%以上100%以下を有効範囲とする。
- 空欄の割引は計算対象に含めない。
- 数値として解釈できない入力は計算しない。
- 不正な入力があっても、画面をクラッシュさせない。
- 割引率100%の場合、該当段階の価格は0円とする。
- 100%割引後の次の割引は、0円に対して適用する。

## 9. UI仕様

### 9.1 基本方針

- 少ない操作で計算できること
- 計算結果が一目で分かること
- 計算過程を視覚的に確認できること
- PC・スマートフォンの両方で利用できること
- 画面遷移をできるだけ減らすこと

### 9.2 入力画面

最低限、以下を配置する。

- 通常価格入力欄
- 割引率入力欄
- 割引追加操作
- 計算結果表示

複数の割引率を入力できること。

### 9.3 結果画面

最低限、以下を表示する。

- 元価格
- 各割引率
- 各段階の掛け率
- 各段階の割引後価格
- 最終支払額
- 実質最終割引率

## 10. 通信・プライバシー

- 基本的な計算処理はブラウザ内で完結する。
- 外部APIへの依存を必須としない。
- 入力された価格・割引率を外部サーバーへ送信することを前提としない。
- 不要なユーザーデータを収集しない。

## 11. 非機能要件

- 主要な最新Webブラウザで動作することを目標とする。
- PCおよびスマートフォンで利用できること。
- 入力変更時に速やかに結果を反映できること。
- 基本計算は外部サービス停止の影響を受けない構成を優先する。

## 12. 開発方針

V1で完結する単機能ツールとして設計する。

機能数を増やすことよりも、

**「割引を順番に計算し、最終価格と実質割引率を正しく分かりやすく示す」**

ことを最優先とする。

特に、複数割引を単純加算してしまう誤解を防ぐため、数式だけでなく計算過程をUI上で確認できることを重視する。

## 13. ライセンス

MIT License
